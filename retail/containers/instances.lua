-- Held bags, combined or individual: one component reads whatever the
-- game shows. The retail bank is its own frame family and is not covered
-- yet.
WowVision.components.createComponent("containers", {
    key = "bags",
    type = "RetailBags",
})
